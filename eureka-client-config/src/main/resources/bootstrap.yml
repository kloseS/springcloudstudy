spring:
  cloud:
    config:
      name: application
      profile: dev
      uri: http://localhost:8001/
      label: master
      discovery:
        enabled: true
        service-id: spring-cloud-config-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:18011/eureka/

